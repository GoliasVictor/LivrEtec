//Preencha esse arquivo e então Renomeie esse arquivo para appsettings.json
//Exemplo em ./src/Docker/GIB.Servidor/appsettings.json
{
  "AuthKey": "", // Insira aqui uma chave para a geração do token de autenticação 
  "ConnectionStrings": {
    "MySql": "server=;database=;user=;password=;" // Insira aqui a string de conexão do mysql
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Kestrel": {
    "EndpointDefaults": {
      "Protocols": "Http2"
    }
  }
}
